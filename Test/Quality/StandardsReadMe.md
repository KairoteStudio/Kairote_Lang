# 开发规范整改验证

本次依据 [DevStand.md](../../DevStand.md) 检查 PR #12 的改动。之前的审计只覆盖编译警告、制表符、尾随空白和文件末尾换行，不能据此宣称通过完整开发规范检查。

| 条款 | 修正 |
|---|---|
| 1.1 基本格式 | 使用统一 K&R、4 空格配置，展开单行控制块及函数；清理新增语法测试的尾随空格。 |
| 1.2 命名 | 修正内部函数的驼峰名称与新增宏的非 `KRT_` 前缀；新增跨文件 Parser 接口采用 `Krt` 前缀；文档使用 `ReadMe.md`。 |
| 1.3、8.1 注释 | 移除临时调试输出，补充新接口的用途、所有权、失败返回及致命错误行为。 |
| 1.4 代码组织 | 检查函数与文件长度；既有 ELF 超长函数在下文报备。 |
| 2.1 测试组织 | 新增组件测试及 12 个新增语法用例使用 `test_` 前缀，保留既有测试编号并同步调用路径。 |
| 2.2 提交范围 | 407 个生成产物、日志和重复实验快照移出提交并保留本地；JSON、ASM、KRO 继续忽略。 |
| 3.2、3.4 错误及内存管理 | 检查新增分配与扩容，失败时保留旧指针或按契约报错；修复前端构造/克隆清理、PE 分配失败清理和寄存器压力下的列表越界。 |
| 5.1 提交检查 | [Audit.py](Audit.py) 将规范检查及空白问题纳入失败退出码。 |

[CheckStandards.py](CheckStandards.py) 检查相对 `main` 的 C 源码、头文件和 `.inc`：共享格式、文件/函数长度、内部函数命名、新增宏前缀、新接口命名与注释、头文件保护及生成产物。历史公开接口按现有兼容性保留。机械检查之外，内存所有权、错误传播与模块职责由代码审查和运行回归验证。

```bash
python3 Test/Quality/CheckStandards.py --base main
python3 Test/Quality/Audit.py --jobs 4
python3 Test/Quality/RunTests.py
```

2026-09-13 的构建与回归结果：

- ArkLink、项目要求的 Zig、CMake Release 和 ASan/UBSan 构建全部通过。
- GCC 与 Clang 各检查 65 个编译单元，零警告、零错误。
- 原生及 ASan/UBSan 编译器分别运行 O2/O3，四组各 256 项，共 1024 项通过；每组包含 181 项整数、指针、语言与性能回归及 75 个语法用例。
- 6 组 C 组件回归在 ASan/UBSan 下通过，覆盖内存、IR、链接器、ELF 失败、后端失败、前端失败；3 项原生质量回归与 12 项规范检查器回归通过。
- 新增前端故障注入逐一检查 22 个语义分析器构造分配点、10 个 AST 克隆分配点；后端检查 30 个临时值的完全干涉图、3 类致命分配失败及 PE 的 18 个分配点，并断言剩余分配数归零。
- 6 个性能负载重新编译后，生成的 `.text` 与规范整改前逐字节一致。本轮未重测耗时，历史性能报告的数字及哈希只描述当时的编译器。

Sanitizer 检查编译器、链接器及 C 组件；生成程序运行原生断言。LSan 未启用。完整分组结果、日志和源码清单位于本地忽略目录 `StandardsValidation/`；本报告保留可审查的验证摘要。

本轮原生编译器 SHA-256：`14e8d0088123701b9e84699f55b5c4a42c3bdf7e23e9552f9d3bbf3066707b0f`。插桩编译器 SHA-256：`615ebf9e645252fc45ff37604197ebe430f0b89a498abba0074e652d25a62347`。

## 按 1.4 条款报备的既有超长函数

`ArkLink/src/Backend/BackendElf.c` 的 `ark_backend_elf_link` 为 1619 行，基线中已超过 500 行。本轮保留其 ELF 构建阶段与状态组织，避免将错误处理修正扩大为整条 ELF 输出布局重构；相关清理路径已通过故障注入测试。其余本轮检查的函数不超过 500 行，所有文件不超过 5000 行。

既有 X86、IR 优化器与 ArkLink 的公开名称维持兼容，本轮新增 API 按规范命名。上述例外明确列出，不将历史兼容接口和已报备函数伪装为全部符合新命名或长度限制。
